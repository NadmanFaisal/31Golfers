import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  authorizationField: {
    height: 40,
    width: "75%",
    borderWidth: 1,
    borderRadius: 7,
    padding: 4,
    margin: 6,
  },
});

export default styles;
